[pytest]
markers =
    unit: fast tests that avoid external services and heavy database usage
    integration: tests requiring database migrations or multiple subsystems working together
addopts = -ra
